{% extends 'base.html.twig' %}

{% block title %}
	{{ client.prenom }}
	{{ client.nom }}
{% endblock %}

{% block body %}
	<h1>{{ client.prenom }}
		{{ client.nom }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ client.id }}</td>
			</tr>
			<tr>
				<th>Prenom</th>
				<td>{{ client.Prenom }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ client.Nom }}</td>
			</tr>
			<tr>
				<th>Matériels</th>
				<td>
					{% for materiel in client.materiels %}
						{{ materiel.nom }}
					</br>
				{% endfor %}
			</tr>
		</tbody>
	</td>
</tr></tbody></td></tr></tbody></table><a class="btn btn-primary" href="{{ path('materiel_new') }}" role="button">ajouter un matériel</a></br><a class="btn btn-primary" href="{{ path('client_index') }}" role="button">Retour à la liste de clients</a>{{ include('client/_delete_form.html.twig') }}{% endblock %}
