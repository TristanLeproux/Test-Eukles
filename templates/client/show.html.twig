{% extends 'base.html.twig' %}

{% block title %}
	{{ client.prenom }}
	{{ client.nom }}
{% endblock %}

{% block body %}
	<h1>{{ client.prenom }}
		{{ client.nom }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ client.id }}</td>
			</tr>
			<tr>
				<th>Prenom</th>
				<td>{{ client.Prenom }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ client.Nom }}</td>
			</tr>
			<tr>
				<th>Matériels</th>
				<td>
					{% for materiel in client.materiels %}
						{{ materiel.nom }}
					</br>
				{% endfor %}
			</tr>
		</tbody>
	</td>
</tr></tbody></td></tr></tbody></table><a href="{{ path('materiel_new') }}">ajouter un matériel</a></br><a href="{{ path('client_index') }}">Retour à la liste de clients</a>{{ include('client/_delete_form.html.twig') }}{% endblock %}
