{% extends 'base.html.twig' %}

{% block title %}Materiel
{% endblock %}

{% block body %}
	<h1>Materiel</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ materiel.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ materiel.Nom }}</td>
			</tr>
			<tr>
				<th>Prix</th>
				<td>{{ materiel.Prix }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('materiel_index') }}">Retour à la liste des matériels</a>
</br>
<a href="{{ path('materiel_edit', {'id': materiel.id}) }}">Modifier</a>

{{ include('materiel/_delete_form.html.twig') }}{% endblock %}
